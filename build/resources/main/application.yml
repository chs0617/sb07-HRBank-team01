spring:
  application:
    name=sb07_HRBank_team01

  datasource:
    url: jdbc:postgresql://localhost:5432/discodeit
    username: discodeit_user
    password: discodeit1234
    driver-class-name: org.postgresql.Driver

jpa:
  database: postgresql
  hibernate:
    ddl-auto: create

  properties:
    hibernate:
      show_sql: true  # SQL ?? ?? ??
      format_sql: true # ??? ?? ?? ???
      default_batch_fetch_size: 100  # n+1 ??? ???? ??? size?? ??? ???. ???? ?? ??

  open-in-view: false